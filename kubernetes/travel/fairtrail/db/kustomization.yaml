apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: travel

resources:
  - scheduled-backup.yaml

helmCharts:
  - name: cluster
    repo: https://cloudnative-pg.github.io/charts
    releaseName: fairtrail-cnpg
    namespace: travel
    version: 0.6.0
    valuesFile: values.yaml
