apiVersion: v1
kind: Service
metadata:
  name: minio-external-service
  namespace: networking
  labels:
    app: minio-app
spec:
  ports:
    - name: minio-console
      protocol: TCP
      port: 9090
      targetPort: 9090
    - name: s3
      protocol: TCP
      port: 9000
      targetPort: 9000
