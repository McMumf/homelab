apiVersion: v1
kind: Service
metadata:
  name: grampsweb
  labels:
    app.kubernetes.io/name: grampsweb
    app.kubernetes.io/instance: grampsweb
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: http
      protocol: TCP
      name: http
