apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: traccar-5055-ingress
  namespace: apps
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - loc.mcmumf.dev
      secretName: loc.mcmumf.dev-tls
  rules:
    - host: loc.mcmumf.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: external-traccar
                port:
                  number: 5055
