engines:
  local:
    lvm:
      enabled: false
    zfs:
      enabled: false

localpv-provisioner:
  localpv:
    nodeSelector:
      openebs.io/engine: mayastor
    tolerations:
      - key: "node-role.kubernetes.io/control-plane"
        operator: "Equal"
        value: ""

mayastor:
  tolerations:
    - key: "node-role.kubernetes.io/control-plane"
      operator: "Equal"
      value: ""
  csi:
    node:
      initContainers:
        enabled: false
  io_engine:
    envcontext: "iova-mode=pa"
    tolerations:
      - key: "node-role.kubernetes.io/control-plane"
        operator: "Equal"
        value: ""
  etcd:
    nodeSelector:
      openebs.io/engine: mayastor
    tolerations:
      - key: "node-role.kubernetes.io/control-plane"
        operator: "Equal"
        value: ""
  loki-stack:
    enabled: false

preUpgradeHook:
  tolerations:
    - key: "node-role.kubernetes.io/control-plane"
      operator: "Equal"
      value: ""

loki:
  enabled: false
